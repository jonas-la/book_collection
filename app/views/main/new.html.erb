<%= link_to 'Home', root_path %>
<h1>Add Book</h1>

<%= form_with(model: @book, url: main_index_path, method: :post) do |f| %>
  <%= render(partial: "form", locals: {f: f , buttonText: "Create Book"}) %>
<% end %>
